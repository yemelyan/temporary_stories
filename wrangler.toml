# Cloudflare Pages configuration
# This file enables Node.js compatibility for the Workers runtime

compatibility_flags = ["nodejs_compat"]

# Compatibility date (required for some features)
compatibility_date = "2024-01-01"
